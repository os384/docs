import{_ as s,C as n,o as a,c as i,ah as t,b as l,w as r,a as h,E as c,ai as d}from"./chunks/framework.C8Zd3T50.js";const w=JSON.parse('{"title":"os384 Overview","description":"","frontmatter":{},"headers":[],"relativePath":"overview.md","filePath":"overview.md"}'),p={name:"overview.md"};function m(u,e,f,v,g,b){const o=n("Mermaid");return a(),i("div",null,[e[1]||(e[1]=t('<h1 id="os384-overview" tabindex="-1">os384 Overview <a class="header-anchor" href="#os384-overview" aria-label="Permalink to &quot;os384 Overview&quot;">​</a></h1><h2 id="applications" tabindex="-1">Applications <a class="header-anchor" href="#applications" aria-label="Permalink to &quot;Applications&quot;">​</a></h2><p>An os384 application is a web app that runs in the user&#39;s browser, launched by the Loader. Apps live on <strong>channel pages</strong> — content addressed and served from an os384 channel. To open an app, a user follows a link like <code>https://384.dev/#&lt;channel-key&gt;</code>, where the fragment (never transmitted to any server) tells the Loader which channel page to load.</p><p>The <a href="/BhoW6b/loader.html">Loader</a> is a special application that manages the user&#39;s <a href="/BhoW6b/wallet.html">Wallet</a> and launches other applications, providing them with access to their channels and keys. It plays a role similar to that of a Unix shell, Explorer.exe on Windows, or the Launcher program on Android.</p>',4)),(a(),l(d,null,{default:r(()=>[c(o,{id:"mermaid-12",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20P1%20--%3E%20Server%0A%20%20%20%20subgraph%20%22Client%22%0A%20%20%20%20%20%20%20%20subgraph%20P4%0A%20%20%20%20%20%20%20%20%20%20%20%20P1%0A%20%20%20%20%20%20%20%20end%0A%20%20%20%20end%0A%20%20%20%20Server%5B%22%E2%98%81%EF%B8%8F%22%5D%0A%0A%20%20%20%20%25%25%20Server%20(Cloud)%20Styling%0A%20%20%20%20classDef%20cloudFont%20font-size%3A96px%2Cheight%3A120px%2Cwidth%3A140px%2Cline-height%3A120px%2Cstroke%3Anone%2Cfill%3Anone%3B%0A%20%20%20%20class%20Server%20cloudFont%3B%0A"})]),fallback:r(()=>[...e[0]||(e[0]=[h(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=t('<h2 id="channels" tabindex="-1">Channels <a class="header-anchor" href="#channels" aria-label="Permalink to &quot;Channels&quot;">​</a></h2><p><a href="/BhoW6b/channels.html">Channels</a> are the primary communication primitive in os384. They provide secure, end-to-end encrypted communication between participants. Each channel is identified by a public key, or sometimes by the hash of the public key. The user who has the private half of channel&#39;s public key is called the <em><a href="/BhoW6b/glossary.html#owner">channel owner</a></em>. The channel owner has full authority over the channel, and can control who may join the channel, etc. Other members in the channel are identified only by their public key. Thus, the full membership list for the channel consists of the owner public key and the public keys of any other non-owner (&quot;<a href="/BhoW6b/glossary.html#visitor">visitor</a>&quot;) members.</p><p>An os384 application may be connected to several channels at once. For example, when you use a chat app it might create one private &quot;ledger&quot; channel solely for its own use, where it can store configuration data and information on all of its other channels. Then it might have one (or more) channels for each of your chat conversations, where it sends and receives chat messages with your friends.</p><p>The <a href="/BhoW6b/glossary.html#channel-server"><strong>Channel Server</strong></a> implements the backend of the channel API to communicate with clients. It provides endpoints for channel members to send and receive messages and for the channel owner to manage the state of the channel. The channel server stores messages and channel state in an <strong>Ordered Key/Value Store</strong>.</p><h3 id="special-channels" tabindex="-1">Special Channels <a class="header-anchor" href="#special-channels" aria-label="Permalink to &quot;Special Channels&quot;">​</a></h3><p>A <a href="/BhoW6b/wallet.html">Wallet</a> is a special kind of channel used to store cryptographic keys and other secrets. Its private key is computed securely from key material that the user can write down or memorize.</p><h3 id="messages" tabindex="-1">Messages <a class="header-anchor" href="#messages" aria-label="Permalink to &quot;Messages&quot;">​</a></h3><p>Clients communicate with each other by sending <a href="/BhoW6b/messages.html">messages</a> in channels.</p><p>Every message is encrypted with <a href="/BhoW6b/glossary.html#aes-256-gcm">AES-256-GCM</a>, using a key derived by an <a href="/BhoW6b/glossary.html#e2e-encryption-end-to-end-encryption">E2E encryption</a> protocol specified by the channel. The encrypted message is also signed by its sender.</p><p>The unencrypted plaintext contents of a message are encoded in a compact binary serialization format with features similar to JSON, so messages may contain virtually any data, with a maximum size of 65kB. Formatting and interpretation of message contents must be performed entirely by the client-side application before encryption and after decryption, as the server can only see the encrypted payload, the cryptographic signature, the sender&#39;s timestamp for the message, and the sender&#39;s public key.</p><h2 id="shards" tabindex="-1">Shards <a class="header-anchor" href="#shards" aria-label="Permalink to &quot;Shards&quot;">​</a></h2><p><a href="/BhoW6b/shards.html">Shards</a> are the fundamental unit of data in os384. A shard combines content addressing together with encryption to create a secure, shareable reference to a blob of data.</p><p>The <a href="/BhoW6b/glossary.html#storage-server"><strong>Storage Server</strong></a> implements the backend of the storage API to communicate with clients. It provides endpoints for authorized users to store and retrieve shards of encrypted data. Behind the scenes, the storage server stores its shards in an <strong>Object Store</strong>, sometimes called a &quot;blob store&quot;.</p><h2 id="internal-services" tabindex="-1">Internal Services <a class="header-anchor" href="#internal-services" aria-label="Permalink to &quot;Internal Services&quot;">​</a></h2><p>The channel server and the storage server are built on top of three core internal services, outlined here for completeness and because it is critical for achieving the scale, performance, and reliability required in a modern server platform.</p><p>The <strong>Compute Service</strong> provides the compute nodes that execute the server-side programs for the channel server and the storage server.</p><p>The <strong>Ordered Key/Value Store</strong> stores messages and state for the channel server.</p><p>The <strong>Object Store</strong> stores shards for the storage server.</p><p>Our open source prototype development instance builds on services from Cloudflare.</p><h2 id="base-infrastructure" tabindex="-1">Base Infrastructure <a class="header-anchor" href="#base-infrastructure" aria-label="Permalink to &quot;Base Infrastructure&quot;">​</a></h2><p>The three core internal services run on top of a base infrastructure layer. It uses distributed hash tables, consensus protocols, and other standard techniques to map the core services onto a set of commodity hardware nodes to provide load balancing, redundancy, and failover.</p><p>We currently use Cloudflare&#39;s base infrastructure as a result of building on their core services, but commodity cloud platforms from Amazon, Google, Microsoft, and others all provide similar facilities. Those who wish to run their own infrastructure can use &quot;cloud native&quot; tools from the open source community.</p><h3 id="client-side-computing" tabindex="-1">Client-side Computing <a class="header-anchor" href="#client-side-computing" aria-label="Permalink to &quot;Client-side Computing&quot;">​</a></h3><p>Whereas conventionally the &#39;platform&#39; layer resides on servers, the os384 architecture migrates most platform functions into the client (browser). It still talks to servers, somewhere, but entirely without any notion of a centralized authority.</p><p>Note that this design rectifies the original limitation of web application services that led to Big Tech consolidation in the first place. Namely, that server applications were too complex, with too many dependencies and too much attack surface. Operating those services effectively on a hostile Internet was prohibitively expensive because it required constant toil by skilled IT staff. In contrast, 384 backed services are extremely lean and simple, with minimal dependencies and little attack surface.</p><h3 id="backend-services" tabindex="-1">Backend Services <a class="header-anchor" href="#backend-services" aria-label="Permalink to &quot;Backend Services&quot;">​</a></h3><p>A complete 384 backed consists of only two services, the <em>channel server</em> and the <em>storage server</em>. These in turn require only an ordered key-value store and an object (&quot;blob&quot;) store.</p><p>The code for these two services is small enough that it can be audited for security or re-implemented in a different language by a small team. Excluding library code, the channel server consists of fewer than 2500 lines of Typescript, and the storage server is even smaller with under 500 lines. For comparison, servers for the Matrix encrypted chat protocol are much more complex and heavyweight, with typically over a hundred thousand lines of code in a high-level language.</p><p>The 384 backend services are a natural fit for modern commodity clouds including so-called &quot;serverless&quot; platforms such as Cloudflare Workers. (In fact, our development prototype instance currently runs on Cloudflare.) The strategy here is simple: Use the public commodity cloud platforms for the things they are good at - building data centers, purchasing compute servers, replacing failed hard disks, minimizing costs, etc - but do not let them read your messages or look at your family photos.</p><p>At the same time, the 384 backend services are also easy to run stand-alone on a traditional server. To make this even simpler, we provide a single Docker image that packages both the channel server and the storage server in one container.</p><p>Regardless of where the backend services are deployed, the 384 HTTP API is designed to make load balancing easy for horizontal scalability.</p><h2 id="development" tabindex="-1">Development <a class="header-anchor" href="#development" aria-label="Permalink to &quot;Development&quot;">​</a></h2><p>Source for all os384 components lives at <a href="https://github.com/os384" target="_blank" rel="noreferrer">github.com/os384</a>.</p>',33))])}const k=s(p,[["render",m]]);export{w as __pageData,k as default};
