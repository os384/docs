import{_ as t,o as a,c as o,ah as i}from"./chunks/framework.C8Zd3T50.js";const u=JSON.parse('{"title":"What About IPFS?","description":"","frontmatter":{},"headers":[],"relativePath":"whatabout/ipfs.md","filePath":"whatabout/ipfs.md"}'),n={name:"whatabout/ipfs.md"};function r(s,e,c,d,h,l){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="what-about-ipfs" tabindex="-1">What About IPFS? <a class="header-anchor" href="#what-about-ipfs" aria-label="Permalink to &quot;What About IPFS?&quot;">​</a></h1><p>IPFS (the InterPlanetary File System) is a content-addressed, peer-to-peer protocol for storing and sharing data. It is philosophically adjacent to os384&#39;s storage layer — both use content-addressing, both aim to decentralize data storage, and both draw on the Merkle tree lineage that runs from Git through to modern distributed systems.</p><p>But IPFS solves a different problem than os384, and it has fundamental limitations in the areas os384 cares most about.</p><h2 id="no-encryption" tabindex="-1">No Encryption <a class="header-anchor" href="#no-encryption" aria-label="Permalink to &quot;No Encryption&quot;">​</a></h2><p>IPFS stores data in cleartext by default. Any node that hosts or relays content can read it. Content-addressing means that anyone who knows (or guesses) the CID of a piece of data can retrieve it from any node that has it.</p><p>This is by design — IPFS is a distribution protocol, not a privacy protocol. But it means that IPFS alone is unsuitable for private communication or private storage. You can encrypt data before putting it on IPFS, but the protocol provides no encryption primitives, no key management, and no access control. That is left entirely to the application layer.</p><p>os384&#39;s storage layer, by contrast, encrypts all data before it reaches the server. The server holds encrypted shards and cannot inspect their contents. Encryption is not optional — it is the only mode of operation.</p><h2 id="no-privacy" tabindex="-1">No Privacy <a class="header-anchor" href="#no-privacy" aria-label="Permalink to &quot;No Privacy&quot;">​</a></h2><p>IPFS&#39;s DHT (Distributed Hash Table) is a public record of who is requesting what content. Any participant in the network can observe which nodes are requesting which CIDs. This is a significant metadata leak — it reveals not just what data exists, but who is interested in it.</p><p>The peer-to-peer architecture means your IP address is visible to other nodes. There is no anonymity layer built into the protocol. Tools like IPFS over Tor exist but are not standard and introduce significant performance penalties.</p><p>Content pinning — the mechanism for ensuring data persists — creates a public record of which nodes are hosting which content. This is useful for content distribution but devastating for privacy.</p><h2 id="content-addressing-—-a-shared-ancestor" tabindex="-1">Content Addressing — A Shared Ancestor <a class="header-anchor" href="#content-addressing-—-a-shared-ancestor" aria-label="Permalink to &quot;Content Addressing — A Shared Ancestor&quot;">​</a></h2><p>Where IPFS and os384 genuinely share intellectual lineage is in content-addressing itself. The idea that data should be named by <em>what it is</em> (its hash) rather than <em>where it is</em> (a URL) is powerful and important. IPFS popularized this concept beyond the Git community and demonstrated that it could work at scale.</p><p>os384&#39;s shard IDs are content addresses — the ID is derived from a hash of the encrypted data. But os384 adds encryption before hashing, privacy-preserving deduplication, and a storage budget system that decouples hosting from content knowledge. These are the properties IPFS does not provide.</p><h2 id="filecoin-and-the-incentive-layer" tabindex="-1">Filecoin and the Incentive Layer <a class="header-anchor" href="#filecoin-and-the-incentive-layer" aria-label="Permalink to &quot;Filecoin and the Incentive Layer&quot;">​</a></h2><p>Filecoin, the incentive layer built on top of IPFS, introduces a blockchain-based market for storage. Miners are paid to host data, and cryptographic proofs verify that they are actually storing it.</p><p>This solves a real problem — IPFS has no built-in incentive for nodes to host other people&#39;s data — but it introduces blockchain complexity, cryptocurrency dependencies, and a cost model that is difficult to predict. The proof-of-storage mechanism requires significant computational resources, and the economics have not proven stable.</p><p>os384 takes a simpler approach: storage is budgeted through channels, tokens are issued and consumed, and the cost model is direct and predictable. No blockchain required.</p><h2 id="what-ipfs-gets-right" tabindex="-1">What IPFS Gets Right <a class="header-anchor" href="#what-ipfs-gets-right" aria-label="Permalink to &quot;What IPFS Gets Right&quot;">​</a></h2><ul><li>Content-addressing is the right foundation for distributed storage.</li><li>The protocol is genuinely decentralized and permissionless.</li><li>The ecosystem has driven significant research in distributed systems, CRDTs, and peer-to-peer networking.</li><li>The project is open source (MIT/Apache 2.0).</li></ul><p>But IPFS is a content distribution protocol, not a privacy or security system. By os384&#39;s criteria, it fails on privacy (no encryption, public DHT, IP exposure), security (no built-in encryption or key management), and — for Filecoin — on simplicity (blockchain dependency). IPFS and os384 share a philosophical ancestor in content-addressing, but they diverge on everything that follows from it.</p>',21)])])}const y=t(n,[["render",r]]);export{u as __pageData,y as default};
