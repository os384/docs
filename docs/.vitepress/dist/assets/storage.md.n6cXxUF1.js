import{_ as c,C as o,o as r,c as p,ah as l,j as t,a,E as s,b as u,w as h,ai as f}from"./chunks/framework.C8Zd3T50.js";const E=JSON.parse('{"title":"Storage (Shard) Server Basics","description":"","frontmatter":{},"headers":[],"relativePath":"storage.md","filePath":"storage.md"}'),g={name:"storage.md"};function m(b,e,y,v,k,w){const n=o("FigureRef"),i=o("Figure"),d=o("Mermaid");return r(),p("div",null,[e[7]||(e[7]=l('<h1 id="storage-shard-server-basics" tabindex="-1">Storage (Shard) Server Basics <a class="header-anchor" href="#storage-shard-server-basics" aria-label="Permalink to &quot;Storage (Shard) Server Basics&quot;">​</a></h1><p>Data storage in os384 is in the form of <a href="/BhoW6b/glossary.html#padding">padded</a>, encrypted, immutable, &quot;anonymous&quot; blobs of data each restricted to a server-specified range of sizes.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup> Very large amounts of data thus needs to first be divided into one or more such chunks; we will cover that later. The final (at rest) storage is in the form of encrypted chunks called <a href="/BhoW6b/glossary.html#shard">Shards</a>. Hence &quot;<a href="/BhoW6b/glossary.html#storage-server">Storage Server</a>&quot; is sometimes referred to as &quot;Shard Server&quot;.</p>',2)),t("p",null,[e[0]||(e[0]=a("There is a process for accepting (ingesting) new data into the storage system, which is somewhat complex and also involves the ",-1)),e[1]||(e[1]=t("a",{href:"/BhoW6b/glossary.html#channel-server"},"Channel Server",-1)),e[2]||(e[2]=a(', which we will get to below. First we look at the basics of what "storage at rest" looks like, in ',-1)),s(n,{id:"basic-storage"}),e[3]||(e[3]=a(".",-1))]),e[8]||(e[8]=t("p",null,[a('Once accepted and stored, the Storage Server simply maintains a mapping of "ID" to the data. To read the data, a client would provide both the ID and the verification string. The server looks up the data, confirms the verification string, and returns the shard.'),t("sup",{class:"footnote-ref"},[t("a",{href:"#fn2",id:"fnref2"},"[2]")])],-1)),t("p",null,[e[4]||(e[4]=a('This makes it trivial to implement a "read only" server, also called a "cache" or "mirror". The right side of ',-1)),s(n,{id:"basic-storage-mirror"}),e[5]||(e[5]=a(' shows how a mirror server might store shards in a simple directory on the local hard drive. On "the internet", it can be stored in various ways, and managed with respect to performance vs cost etc.',-1))]),e[9]||(e[9]=t("p",null,[a('The "ID" of the data is a hash function of the data itself, as we will describe next. Thus, any identical piece of information will only be stored once, called "'),t("a",{href:"/BhoW6b/glossary.html#deduplication"},"de-duplication"),a('". However, since we want to deduplicate data in a private manner, this requires a bit of finesse.')],-1)),s(i,{id:"basic-storage",caption:"Basic storage service. Any client need only retain the basic parts of an Object (refer to Figure 2). The server simply keeps blobs of data indexed by the shard ID, an associated verification string that is server-specific, and any additional cryptographic materials, in this case a nonce and a salt.",src:"/images/storage_06a.jpg",align:"center",width:"90%"}),s(i,{id:"basic-storage-mirror",caption:"Storage mirror service implementation example, showing a simple directory structure for storing and retrieving shards.",src:"/images/storage_06b.jpg",align:"center",width:"90%"}),e[10]||(e[10]=t("p",null,'The diagram below shows the basic interchange with the storage server: Alice wants to send some data to Bob. At the core of the storage protocol, Alice collaborates with the storage server to derive an encryption key, then uploads that encrypted data, and sends the Object to Bob (labelled "[5] Manifest" in the figure).',-1)),(r(),u(f,null,{default:h(()=>[s(d,{id:"mermaid-23",class:"mermaid",graph:"sequenceDiagram%0A%20%20%20%20participant%20Bob%0A%20%20%20%20participant%20Alice%0A%20%20%20%20participant%20Storage%0A%0A%20%20%20%20Alice-%3E%3EStorage%3A%20(1)%20First%20hash%0A%20%20%20%20Storage--%3E%3EAlice%3A%20(2)%20Encryption%20params%0A%20%20%20%20Alice-%3E%3EStorage%3A%20(3)%20Second%20hash%20and%20data%0A%20%20%20%20Storage--%3E%3EAlice%3A%20(4)%20Verification%0A%20%20%20%20Alice-%3E%3EBob%3A%20(5)%20Manifest%0A"})]),fallback:h(()=>[...e[6]||(e[6]=[a(" Loading... ",-1)])]),_:1})),s(i,{id:"storage-metadata",caption:"Example of (minimum) metadata kept by storage server (per shard)",src:"/images/storage_08.jpg",align:"center",width:"90%"}),s(i,{id:"shard-hexdump",caption:"Sample hex dump of a shard.",src:"/images/storage_09.jpg",align:"center",width:"90%"}),e[11]||(e[11]=l(`<p>Most existing online storage systems can only effectively deduplicate unencrypted data. This leads to significant cost increases. The reality is that &quot;most&quot; data that individuals and companies store are copies – consider a set of PDF documents that might be related to some real estate transaction: there will be multiple parties involved, that all have various subsets of the exact same files.</p><p>Deduplicating online storage of data under the constraints of security (encryption) and privacy (anonymization) has been the subject of many years of research, and a few basic approaches have been suggested, but at the time that we began work on os384, to our knowledge there were no working services.</p><p>Since this characteristic (secure, private, but the server-side can still deduplicate) is unusual, let&#39;s summarize our requirements on a modern &quot;web3&quot; storage system:</p><ul><li>The system should be highly secure and private.</li><li>Data should be strongly encrypted. There should be no way for network or server to inspect contents.</li><li>For any particular piece of data, it should be difficult to determine who first uploaded a copy.</li><li>It should be difficult to determine who has downloaded copies.</li><li>It should be very difficult to determine who &quot;shares&quot; (or &quot;links&quot;) a copy, e.g., provided the necessary information that allowed a party to download.</li><li>It must be possible to allocate the cost of storage, such that regardless of scale of the system, it does not rely on any particular user behavior (e.g. peer hosting).</li><li>Any information about &quot;traffic&quot; related to uploads, downloads, etc., should be ephemeral.</li><li>It should be difficult to determine if a piece of data exists on the system, e.g., if it has been uploaded.</li><li>The system must be fundamentally capable of scalable deduplication of data.</li><li>Names of chunks of data should be universal, and not tied to any particular server or source.</li><li>It should be possible, and simple, for any individual host or server to block (&quot;blacklist&quot;) any particular chunks of data, subject to their choice of hosting policies.</li><li>Finally, the system shouldn&#39;t depend on any external sources of naming or authentication. In other words, clients should be able to validate any data regardless of trust of any other parties it communicates with.</li></ul><p>Let&#39;s walk through how os384 accomplishes storage to match these requirements.</p><p>We will begin with a simple case, such as a small JPEG image, with no real need for additional information. A more complex case, such as backing up a large directory of files, will require other information to be handled – e.g. the list of files itself, for each file a list of one or more chunks and additional meta data like file names and last modified dates, etc. Beyond that, we can cover how things like &quot;applications&quot; are handled. The storage server per se is unaware of any of these &quot;details&quot;. We will cover them later when we discuss the File System (&quot;<a href="/BhoW6b/glossary.html#sbfs-os384-virtual-file-system">SBFS</a>&quot;).</p><p>For now, assume we just want to store a single piece of data, small enough to fit into a single shard:</p><ol><li>The client first packages any data to be saved into an ArrayBuffer.<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup> From the perspective of an app developer, more or less any valid JavaScript object can be stored, as its first &quot;package&quot;.<sup class="footnote-ref"><a href="#fn4" id="fnref4">[4]</a></sup></li><li>This data is next padded to prescribed lengths. The purpose is to reduce information leakage inherent in the size of, for example, underlying files.<sup class="footnote-ref"><a href="#fn5" id="fnref5">[5]</a></sup></li><li>The padded data is hashed with SHA-512 (producing 64 bytes), which is split into two 32-byte halves: h1 (the first half, used as the server-side lookup key) and h2 (the second half, used as the client-side encryption key material). The server never sees h2.</li><li>Next the client sends the first hash to the storage server, see the sequence diagram above. In that diagram, Alice sends h1 (&quot;[1]&quot;), and then receives encryption parameters.</li><li>The server keeps a separate table translating &quot;h1&quot; hashes to verification, 16-byte <a href="/BhoW6b/glossary.html#salt">salt</a>, and 12-byte <a href="/BhoW6b/glossary.html#nonce">nonce</a> (also called &quot;<a href="/BhoW6b/glossary.html#iv-initialization-vector">iv</a>&quot;). Thus, every user who has the same piece of data &quot;D&quot;, will be deriving the same &quot;h1&quot; and hence receiving the same verification, salt, and nonce.</li><li>If a particular value of h1 has not been seen, the server will generate random values. The client can&#39;t see the difference between an old and a new match. Furthermore, the server has a &quot;<a href="/BhoW6b/glossary.html#privacy-window">privacy window</a>&quot; (currently set to 14 days), which resets this data for any given h1.</li><li>The client then takes the second hash (h2) and the salt and 100,000 iterations of <a href="/BhoW6b/glossary.html#pbkdf2-password-based-key-derivation-function-2">PBKDF2</a> (with SHA-256) to derive an encryption key (<a href="/BhoW6b/glossary.html#aes-256-gcm">AES-GCM</a> length 256). Note: this is lower than the 10 million iterations used for wallet key derivation — h2 is already high-entropy hash material, not a human password, so fewer iterations are needed.</li><li>Next the client encrypts the padded data with this key and the provided nonce (&quot;iv&quot;).</li><li>The client then takes the size of this encrypted buffer and obtains a &quot;storage token&quot; to match the size.<sup class="footnote-ref"><a href="#fn6" id="fnref6">[6]</a></sup></li><li>A SHA-256 hash is taken of the concatenation of (iv + salt + encrypted buffer). This is the final content address (storage ID) of the shard — a third hash, derived from the encrypted form, distinct from h1 and h2.</li><li>The encrypted data can now be uploaded to the server – the client provides the final identifier, nonce, salt, storage token, and the data itself.</li><li>The server now validates all the different parts: it confirms that the token is valid (and marks it &quot;consumed&quot;), and uses the nonce, salt, and data to verify the object identifier.<sup class="footnote-ref"><a href="#fn7" id="fnref7">[7]</a></sup></li><li>Once all validated, the server responds with success, and the <a href="/BhoW6b/glossary.html#verification">verification</a>. The client needs to hold on to that value, since it demonstrates that this storage has been paid for. The result of this protocol is the object in Figure 2. Everything else can be discarded. The data the server will return upon a future query will include the nonce and salt, so the client does not need to track that.</li></ol><p>One important design property follows from the encryption scheme: the storage server can enforce a content policy — for example, responding to lawful takedown requests — by overwriting a shard. What it cannot do is pre-emptively scan or inspect the contents of any stored shard. The design deliberately preserves this distinction.</p><p>This covers the basics of the storage server. The next main building block is the Channel Server.</p><h2 id="storage-budgets" tabindex="-1">Storage Budgets <a class="header-anchor" href="#storage-budgets" aria-label="Permalink to &quot;Storage Budgets&quot;">​</a></h2><p>Storage in os384 is not free-floating: every shard upload must be authorized by a <strong>storage token</strong>, a one-time-use spend credential. Tokens are issued by Channels out of their storage budget.</p><p>Each Channel holds a <code>storageLimit</code>, a byte count representing its current storage budget. This is persisted in the Channel&#39;s own Durable Object storage. When a client wants to upload data, it first requests a token from the channel:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /api/v2/channel/&lt;id&gt;/getStorageToken?size=&lt;bytes&gt;</span></span></code></pre></div><p>The channel debits its <code>storageLimit</code> by the requested size and creates an <code>SBStorageToken</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  hash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// random unique token identifier</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// approved byte count (after padding)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  motherChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ChannelId,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  created</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// timestamp</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  used</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The channel writes this record directly into a shared <code>LEDGER_NAMESPACE</code> KV store (accessible to both the Channel and Storage servers), then returns the token to the client.</p><p>The client includes the token&#39;s <code>hash</code> in its <code>storeData</code> request. The storage server looks up the token in <code>LEDGER_NAMESPACE</code>, then verifies: the token is not already used, the upload size exactly matches <code>token.size</code>, and the shard ID is consistent with the data. On success, the server marks <code>used: true</code> in <code>LEDGER_NAMESPACE</code> — the token is spent and cannot be reused.</p><p>There is no separate ledger process. The channel server is the ledger: it holds the budget, issues tokens, and the storage server validates against the same shared KV store.</p><h3 id="bootstrap" tabindex="-1">Bootstrap <a class="header-anchor" href="#bootstrap" aria-label="Permalink to &quot;Bootstrap&quot;">​</a></h3><p>On a personal or self-hosted server, storage budget can be inserted directly without going through a channel. A token can be written straight into <code>LEDGER_NAMESPACE</code> via the CLI:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wrangler</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kv:key</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> put</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --binding=LEDGER_NAMESPACE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;&lt;token-hash&gt;&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;used&quot;:false,&quot;size&quot;:33554432}&#39;</span></span></code></pre></div><p>This creates a 32 MB spend token that any client with the hash can use to upload data. The CLI (<code>384</code>) provides a wrapper for this that handles key generation.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Current server allows a range of 64 KiB to 4 MiB. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>An &quot;Object&quot; as introduced earlier (Figure 2) identifies a shard, but the (raw) data itself is the &quot;Shard&quot;. <a href="#fnref2" class="footnote-backref">↩︎</a></p></li><li id="fn3" class="footnote-item"><p>In all our specific examples, we will use Typescript, which is the current system language used by OS384. <a href="#fnref3" class="footnote-backref">↩︎</a></p></li><li id="fn4" class="footnote-item"><p>Any JavaScript object that can be serialized (structured clone) can be stored. The library handles packaging into an ArrayBuffer automatically. <a href="#fnref4" class="footnote-backref">↩︎</a></p></li><li id="fn5" class="footnote-item"><p>Exactly how this is done does not matter too much; the purpose is to reduce the set of possible shards, in order to reduce the information leakage inherent in the shard size. The current policy is to round up to closest 4 KiB, after which round up to closest power of 2, doing so up to a size of 1 MiB, and after that round up to closest MiB boundary. <a href="#fnref5" class="footnote-backref">↩︎</a></p></li><li id="fn6" class="footnote-item"><p>This is handled by the Channel Server, and we will cover this in more detail below. Briefly: when you create a Channel, it will have a &quot;budget&quot;, and one its abilities is to &quot;print&quot; storage tokens out of that budget. <a href="#fnref6" class="footnote-backref">↩︎</a></p></li><li id="fn7" class="footnote-item"><p>Note that the client does not need to use the same nonce and salt that the server provided in response to h1. It&#39;s free to generate its own random values. This is a subtle trade-off in privacy. The downside of using server-provided values is that if the server is compromised during the entire operation, it can be traced who &quot;first&quot; uploaded a piece of information. However, the downside of the client using its own random values is to remove deniability – now the object could only have come from that client. In normal operation, the server&#39;s privacy window obfuscates the point of upload from any future points of download. <a href="#fnref7" class="footnote-backref">↩︎</a></p></li></ol></section>`,25))])}const T=c(g,[["render",m]]);export{E as __pageData,T as default};
